package com.jrd.study.service;

@FunctionalInterface
public interface Functionalinterface {
	//interface funcional só pode ter um metodo
	// pode ser criado em uma classe também e já usado
	
   public float apply(float numb1, float numb2);
}
